```jsx
import AddIcon from '@material-ui/icons/Add';
<div>
    <Button color="primary" onClick={() => alert('dfsdf')}>
        Click me!
    </Button>
    <Button color="primary" size="small" onClick={() => alert('dfsdf')}>
        Click me!
    </Button>
    <Button color="primary" fontSize={12} onClick={() => alert('dfsdf')}>
        Click me!
    </Button>
    <Button variant="outlined" color="primary" fontSize={12} onClick={() => alert('dfsdf')}>
        Click me!
    </Button>
    <Button variant="text" color="primary" fontSize={12} onClick={() => alert('dfsdf')}>
        Click me!
    </Button>
    <br />
    <br />
    <Button variant="text" color="default" fontSize={12} onClick={() => alert('dfsdf')}>
        Click me!
    </Button>
    <Button variant="text" color="secondary" fontSize={12} onClick={() => alert('dfsdf')}>
        Click me!
    </Button>
    <Button variant="outlined" color="secondary" fontSize={12} onClick={() => alert('dfsdf')}>
        Click me!
    </Button>
    <Button variant="contained" color="secondary" fontSize={22} onClick={() => alert('dfsdf')}>
        Click me!
    </Button>
</div>;
```
