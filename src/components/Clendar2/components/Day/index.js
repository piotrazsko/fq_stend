import React from 'react';
import PropTypes from 'prop-types';
// import Grid from '../../../Grid';
import style from './style.module.scss';

const Day = ({ curentDay, setCurentDay, setShowTime, selectedDate, interval = 15 }) => {
	return <div></div>;
};

Day.propTypes = {
	// : PropTypes.
};

export default Day;
